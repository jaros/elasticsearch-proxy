# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

# elasticSearch proxy mappings

GET       /index_info/:indexName      controllers.Application.indexInfo(indexName)

GET       /es/*action                 controllers.Application.esProxy(action)
POST      /es/*action                 controllers.Application.esProxy(action)
PUT       /es/*action                 controllers.Application.esProxy(action)
DELETE    /es/*action                 controllers.Application.esProxy(action)
